# 技能港

SkillHarbor 技能港：中文优先的个人全局技能管理平台。目标是“全局统一维护技能 → 查看中文含义与用法 → 选择项目 → 创建软链接”。全局技能库是默认首页，项目管理的是技能引用；本地 CLI 执行属于辅助能力。

## 本地开发

需要 Node.js、Rust/Cargo、平台 Tauri 构建依赖；执行与中文生成另需已安装并登录的 Codex CLI。本次验证使用 macOS Apple Silicon、Node 24、Rust 1.98.1、Codex CLI 0.144.1。

```sh
npm ci
npm run workbench:dev
```

打开设置中的“本地执行与中文说明”，配置 Codex 可执行文件路径（留空自动查找）。可单独指定项目任务模型，留空沿用 CLI 配置；这不会修改全局 Codex 配置。若 CLI 报模型要求更新版本，升级 CLI 或填写一个当前 CLI 支持的模型。

```sh
npm run workbench:build
```

macOS 应用产物位于 `src-tauri/target/release/bundle/macos/SkillHarbor.app`。可加 `-- --debug` 生成开发构建，产物位于 `target/debug/bundle/macos`。应用使用独立应用标识，不启用自动二进制更新。

## 使用流程

1. 默认首页“全局技能”展示中央库全部技能，点击技能查看中文用法或原文；“维护与更新”保留标签、更新和批量管理。
2. 在“发现技能 → 本地安装”选择目录或输入 SKILL.md 所在目录；也可以通过市场或 Git 导入。
3. 在全局技能中多选，或打开技能详情，点击“链接到项目”后选择项目和工具，创建软链接。没有项目时先新建或导入项目目录。普通添加流程固定使用软链接，不再提供副本选项；链接失败明确报错，不退化为复制。
4. 在全局库或项目中选择技能，查看原文或生成中文用法。可编辑中文说明，原文更新后显示过期提示；项目副本按项目实际文件生成。
5. 项目需要执行时，点击“用技能开始任务”，填写中文任务。当前执行器为 Codex CLI，任务在所选项目目录内启动。
6. 查看可读的任务结果或展开诊断日志。可以停止任务；项目的运行记录保留在本机。
7. 市场可将中文需求转换为英文检索词、预览真实源文的中文用法，并选择安装后链接到指定 Codex 项目。

## 数据和行为

- 沿用中央技能库、SQLite 项目记录和原同步引擎。项目绑定与执行偏好作为 SQLite settings 增量保存，无破坏性数据库迁移。
- 中文说明与任务记录在 `~/.skillharbor/local-workbench/`，独立于技能 Git 备份；原始 SKILL.md 不被翻译覆盖。它们属于本机数据，不随现有备份自动跨设备恢复。
- 全局只维护一份技能，项目软链接共享源内容。既有副本保留兼容，不自动转换或删除。移出项目不删除中央库源文件。CLI 仍可能发现全局技能，项目列表不代表隔离环境。
- 同一应用实例内，同项目一次只运行一个任务。取消和正常退出会终止受管理进程树；异常崩溃后标记“已中断”，不假装任务成功。异常退出后需检查可能残留的 CLI 进程再重试。
- 不启用跳过审批/沙箱的执行参数。需要交互登录或权限处理的任务会显示失败日志，需在终端完成配置后重试。首版不包含内嵌交互式终端或会话续接。
- 中文生成通过当前 CLI 默认模型，隔离目录、只读沙箱并禁用已验证的工具开关。原文作为数据传入；结果需要人工核对。
- 市场中文预览当前支持公开 GitHub 来源；目录歧义、过大文档、API 限流会明确报错，不根据名称推测用途。

## 验证

```sh
npm run lint
npm run build
cargo test --manifest-path src-tauri/Cargo.toml --lib
```

端到端验证用专门的临时目录运行开发构建：

```sh
SKILLHARBOR_SANDBOX_DIR=/tmp/my-workbench-check npm run workbench:dev
```

该变量仅开发构建支持，将应用数据与本机技能库隔开；Agent 检测仍会读取本机配置。不要在验收时点击全局批量部署。测试项目中的文件操作应限定在临时目录。

详细交付与真实验收记录见 `WORKBENCH-VERIFICATION.md`。

## 界面风格

按本地 OpenDesign 源码中的设计令牌、应用壳层、组件和设置布局重设计。采用白色画布、浅灰侧栏、Albert Sans 与中文系统字体、细边框、紧凑控件和深色主按钮。默认首页为全局技能，支持搜索、来源与标签筛选、排序、网格/列表和批量链接。详情优先中文用法；项目页管理引用；设置按五个分类展示。新用户默认浅色，已有主题偏好保留。

设计方案见 `DESIGN.md`，源码映射及实际界面验收见 `design-qa.md`。字体和参考样式授权见 `THIRD_PARTY_NOTICES.md`。

### 规约落实到组件

公共PageHeader/Button负责一致的页面标题与操作；DetailSheet负责原生模态与进出动效；独有布局写在CSS Module。全站鼠标点阵只挂载一次。迁入的原文、前端约定和验收分别见 docs/design/OPEN-DESIGN-RULES.md、src/AGENTS.md、design-qa.md。
