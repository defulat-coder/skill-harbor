# 技能港 · 设计方案

状态：基于 Open Design 当前源码持续映射；用户最新决定将默认页改为居中问答首页，详见 docs/design/SEARCH-HOME.md。此前 design-qa.md 是历史验收，不能作为新首页已验收的依据。用户目标优先于参考产品的创作场景。

完整设计规约与执行入口：[docs/design/README.md](docs/design/README.md)。该目录保留完整来源快照与迁移边界；后续前端开发必须遵循根 AGENTS.md。

## 产品目标

在问答首页检索当前中央技能目录，或在 `/library` 浏览全局维护的技能列表 → 查看中文含义、解释与用法及原始出处 → 把选定技能通过软链接加入一个项目。项目只维护引用；市场帮助发现英文技能并理解中文用法。本地 CLI 执行保留为项目的辅助能力。

## 参考依据

参考仓库只读：`/Users/xbjt/Documents/myself/open-design`。

- `skills/impeccable-design-polish/SKILL.md`：审查/批评/打磨/加固，减少空泛装饰，保留可访问性及真实功能。
- `apps/web/src/styles/tokens.css`：白色/中性灰基础，深色主要操作，绿色辅助选中；圆角 4/8/12/16；100/150/200ms 反馈节奏。
- `apps/web/src/styles/base.css`：Albert Sans + 中文字体回退；主体 14px，阅读文字有明确行距。
- `apps/web/src/styles/home/entry-layout.css`：44px 顶部外壳、236px 可折叠导航、浮动导航面板、独立内容滚动。
- `apps/web/src/styles/home/recent-projects.css`：网格/列表、筛选与排序工具条。
- `apps/web/src/styles/primitives.css`：输入框、按钮、统一模态层。
- `apps/web/src/components/SettingsDialog.tsx`：分类导航与设置内容分离。

源码的最新主底色为白色，用户先前截图的灰色视觉不作为全站唯一底色。保留中性灰面板层次，不继续叠加旧版蓝色/偏绿样式。

## 页面与流程

1. **问答首页（`/`）**：参照 HomeHero 居中输入框，中文提问，显示当前中央目录和索引状态，先展示混合检索片段，再生成中文回答；回答失败保留来源。来源范围不包含 Git 仓库、业务项目或公开市场。
2. **全局技能（`/library`）**：24px 页面标题、总数与添加操作；单行搜索，来源/标签/排序；网格与列表切换；支持多选并批量软链接。技能条目显示原名、说明、来源与标签。无虚构示例技能，无营销大标题，不添加没有真实能力支撑的输入控件。
3. **技能详情**：可滚动居中详情，标题区保留“链接到项目”动作；默认中文用法，原文并列可切换；源路径可查看。链接面板有目标项目、CLI 工具与明确的软链接说明，逐项显示成功/失败。
4. **项目引用**：同样的页面标题、工具条、项目卡片或条目。进入项目后左侧技能列表、右侧说明；运行记录作为次级标签。去掉“从项目开始”的大介绍块。
5. **发现技能**：保留市场/本地/Git 三种来源；统一标题、筛选、卡片和安装动作；中文检索与预览仍可用。
6. **维护与更新**：保留标签、批量更新及现有工具开关；共享字体、颜色、按钮、间距，归入技能库下的次级导航。
7. **设置**：页面标题在先，分类导航 + 内容面板；本地执行、工具目录、外观、同步设置各有归属。现有配置不迁移、不重置。
8. **备份与工具目录**：沿用既有功能，接入统一外壳、页面宽度和设计 token。

**索引管理（`/search-index`）**：独立功能页与固定侧栏入口，集中建立/更新/状态/重试。首页只呈现检索状态与跳转入口；任务在页面切换后继续，使用应用共享状态。

## 设计规则

- 颜色：底 #fff，面板 #fafafa，淡灰 #ededed；正文 #494949、标题 #202020、次级 #5c5c5c；主要操作 #353535；绿色 #007106 / #F2FFF2 仅用于状态/选中辅助。暗色沿用参考源码中性灰层级。
- 字体：优先参考项目的 Albert Sans，中文 PingFang SC/Microsoft YaHei；正文14、辅助12/13、分区16/18、页面标题24。中文不采用负字距。
- 形状：控件4/8px、卡片8/12px、大对话框16px；只有标签和明确的圆形工具按钮使用胶囊/圆形。
- 间距：以4px为基准，常用8/12/16/24/32px；统一对齐，不在每页叠加不同容器留白。
- 交互：可见焦点，真实button/select/input，禁用/加载/空/失败/部分成功状态；模态焦点约束与Escape；尊重减少动画偏好。
- 边界：Tauri + React + Rust + SQLite及现有数据模型保留。本轮参考设计系统，不移植 Open Design 的 Next/Electron/生成器架构。

## 验收

构建与lint通过；实际打开全局技能、搜索/筛选/视图切换、技能中文/原文、批量链接入口、项目、市场、设置；检查明暗主题和窄窗口。所有数据操作使用隔离测试目录。记录最终视觉与功能验收，保留未覆盖项。

## 按迁入规约执行的整站重设计

本轮从源 AGENTS 的 CSS 归属、公共控件复用和 UI 动效章节落实到代码：

- PageHeader 与 Button 成为页面公共入口组件，局部样式用 CSS Module；全局、项目、市场、维护、工具目录、备份、设置及项目高级管理采用统一标题层级。
- 全局库保留网格/列表及批量链接，卡片统一阅读区和操作区。项目侧栏与中文阅读面板分层；市场把搜索/中文转换、安装目标与结果分开。
- 备份页以主内容和辅助栏呈现；维护页集中搜索、筛选和更新操作。
- DetailSheet 统一原生模态焦点、关闭和进出动效；确认、标签、预设、Git、帮助、首次恢复、窗口关闭等弹窗复用该组件。
- 侧栏保持挂载，通过状态动画收起；隐藏时 inert，键盘不会进入隐藏导航。公共开关使用 transform 动画和明暗主题适配。
- 全页点阵持续由唯一公共实例提供。业务函数和后端数据模型保持原有语义。

验收记录见 design-qa.md 的本轮章节。现有复杂业务区仍可使用原生语义控件和旧共享类，不要求为形式一致重写全部业务。

## 五轮简化后的交互约束

见 docs/delivery/impeccable-five-rounds/GOAL.md 与 ROUNDS.md。参考原项目的 EntryNavRail（导航而非操作台）、HomeHero（一个提交入口）、RecentProjectsStrip（低频操作放更多）、SettingsDialog（分层设置）：

- 日常主导航为首页、全局技能、项目、发现技能；索引管理独立可达，工具/备份/预设/维护按需展开。
- 同一任务区域只强调一个主要操作。选择、筛选、高级操作按需出现，不能通过删除功能来追求空白。
- 卡片以阅读为主，项目加入在详情或明确的批量模式中完成，完成后提供打开项目。
- 问答结果按技能合并来源，保留引用编号；回到首页保留本次问答。
- 安装活动状态不受过滤影响，忙碌期间固定目标；展开的折叠区允许菜单显示，关闭时隐藏内容不可聚焦。
