# 整站重设计 · 集成验收

日期：2026-09-05。实际测试使用 macOS Tauri/WKWebView 桌面包；隔离根目录 `/tmp/skillharbor-e2e/home`，没有对真实技能目录执行导入、启停、删除或备份写入。源码盘点见 inventory.md，各域报告逐项区分实现审查与实际操作。

## 本轮真实执行

| 流程 | 实际结果 |
| --- | --- |
| 预设管理 | 新侧栏入口可达；创建“设计验收预设”，保存描述；菜单键盘 Down/Enter 上移成功；修改名称为“设计验收已修改”并换图标；嵌套模态关闭返回管理层；菜单关闭恢复焦点到触发器 |
| 快捷查找 | 顶栏可见入口及 Cmd+K；输入 project-note、Enter；打开全局中文详情，原始文档切换、来源折叠及嵌套链接取消可用 |
| 本地导入 | 从 `/tmp/skillharbor-e2e/source/delivery-check` 实际导入，全局库由2项变3项；没有导入扫描出的用户真实技能 |
| 全局选择 | 单选时原生 AX 全选值为2（混合状态），全选后值为1；批量操作可达 |
| 中文说明 | 新 delivery-check 使用本地真实 Codex CLI 生成中文用途、场景、准备、示例、限制；重新打开可读缓存；编辑保存人工中文说明成功 |
| 新项目 | UI创建 `/tmp/skillharbor-e2e/full-delivery-project`，ID `8138f4a5-d22b-4553-8f4a-f9d2e57b5657`，同时选择 delivery-check/project-note；磁盘确认两项均为真实 symlink 指向中央技能 |
| 部分成功 | 临时创建真实 readme-check 同名目录及 keep.txt；批量链接3项返回2成功1冲突；keep.txt内容未变；将测试冲突目录移开后点击“重试失败项”，3项全部成功，再提交按钮禁用 |
| 移除引用 | readme-check点击“移出项目”并确认；项目由3项变2项；磁盘确认链接消失、全局 SKILL.md仍在 |
| CLI任务 | 新项目实际启动 task `6b16077e-af95-4266-a9fa-c478f84abb6f`、模型gpt-5.6-sol，记录/日志可读；CLI停留在turn.started未返回文字，UI停止成功，状态保存为已取消。此轮不声称该任务完成了文件生成 |
| CLI设置错误 | 隔离设置填写不存在的可执行路径后明确显示“设置已保存，但无法连接”；恢复留空并保存，项目模型仍保留gpt-5.6-sol |
| 外观 | 浅色全局详情/项目、深色设置/备份/全局工具/龙虾工具/维护/市场截图实查；窗口拖小后市场搜索、来源和卡片重新排布；未改变系统减少动态效果设置 |
| 鼠标动效 | 上述所有路由沿用同一背景；深色工具页面实际截图可见鼠标附近点阵变化；输入期间抑制、减少动效和隐藏暂停另经源码检查 |
| 备份失败 | 未连接状态布局实查；尝试本地临时bare仓库地址，被现有URL策略明确拒绝且保留输入；未绕过协议约束，未登录GitHub或向远端写数据 |

## 验收时发现并继续修复

- 全局说明和项目链接的独立缓存导致项目重复要求生成：项目尚无说明且实际指向中央源时继承全局说明，项目自己的说明仍优先；副本不继承。后端测试覆盖真实软链接、后续全局更新、项目覆盖与副本隔离。
- DnD把button角色铺到卡片外层，WKWebView AX隐藏内部操作：维护页和设置页均把角色、监听器和activator移到独立原生按钮。
- 长时间CLI无文字反馈不足：加入一分钟后查看日志/停止重试的提示，保留实际状态，不生成假进度。
- 帮助与预设残留英文/旧模型：三语种帮助调整，简繁预设文案与31个图标标签中文化。
- 备份页删除面向开发阶段的说明及尚未实现的100MB限额承诺。

## 边界

本轮完成注册页面与可达操作的设计/状态盘点及实现检查，真实回归采用代表流程和失败场景，不等同于每个后端组合都已人工运行。GitHub OAuth、远端备份/恢复/冲突合并、应用升级、钥匙串、用户真实工具目录批量写入未实测；Windows/Linux未运行。减少动态效果已检查运行时分支及覆盖portal的CSS，未改用户系统偏好进行实际仿真。市场实时内容与CLI模型响应依赖外部服务。

此前隔离任务 `017f7e50-fe70-4a54-a7b4-ce19663596a1` 已实际完成PROJECT_NOTE.md生成，属于上一轮证据，不能替代本轮新任务的完成状态。

构建、最终复验和签名结果由最后集成步骤追加。

## 最后复验补充

- 最终调试包实际确认：项目 delivery-check 直接显示从全局继承的“人工修订”及三段中文内容，没有再次调用模型。
- 维护页 AX 现在分别暴露排序把手、标题、更多菜单、启用开关及工具操作。实际新增“交付验收”标签成功，并打开独立标签菜单。
- 设置工具卡片 AX 分别暴露拖拽把手、启用开关与两项路径编辑；新增工具表单有独立标签，弹窗可关闭。
- 项目高级管理实际打开 AddSkillsSheet，选择 Codex/readme-check后提交成功；磁盘再次确认旧入口生成的是软链接。
- 全部源码字面量 i18n 调用扫描：简中/繁中/英文缺失为0（不计提供defaultValue的调用）；修复高级页暴露raw key的刷新/关闭按钮。
- 最后两处原生 details（执行日志、生成草稿）统一消费 Disclosure，使用同一折叠动效与减少动态效果规则；类型及定向检查通过。
- 完整 Rust 测试：490 passed / 0 failed / 0 ignored，62.53s。来源快照42项哈希全部匹配；git diff --check通过。

## 交付文件与重复验收

- 正式本机应用：`src-tauri/target/release/bundle/macos/SkillHarbor.app`。正式应用使用原有用户数据目录，隔离环境开关仅供调试包使用。
- 隔离预览：`src-tauri/target/debug/bundle/macos/SkillHarbor.app`，启动时设置 `SKILLHARBOR_SANDBOX_DIR=/tmp/skillharbor-e2e/home`。
- 本轮修改未提交Git；保留工作区既有修改。已修复忽略规则，根/src的开发规约及docs/design、docs/delivery可纳入版本管理。
- 正式包为本机ad-hoc签名，不代表已完成Apple公证或跨机器分发验证。

最终 `npm run lint`、`npm run workbench:build`（含TypeScript/Vite）退出0。正式包 `codesign --verify --deep --strict` 退出0。本轮保留现有Vite大chunk提示（主JS约1.08MB），不把构建警告写成运行失败。
